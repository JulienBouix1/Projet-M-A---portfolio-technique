# Screenshots to drop here

The site expects the following files. The `Figure` component falls back to a labelled placeholder tile when a file is missing, so you can ship without them — but adding them changes the site from a written description to visible proof.

| File | Where it appears | What to capture |
|---|---|---|
| `cockpit-hitl.png` | After the HITL section | Screenshot of the Analyst tab in the CRM cockpit, showing the review queue. 16:9 preferred. |
| `medadom-deck-cover.png` | Below the PitchShowcase | Real slide-01 of the generated Medadom pitch deck (PDF export). |
| `valo-football-field.png` | Below the Valuation section | The Synthèse tab of the valuation Excel workbook. |
| `arch-explorer-panel.png` | Below the Architecture explorer | Screenshot of the loupe-effect open on one infra node. |

Format: PNG or WEBP, 1600–2400px wide, under 300KB if possible. The component uses `object-fit: cover` at 16:9.
