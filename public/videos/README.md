# Videos to drop here

| File | Where it appears | What to capture |
|---|---|---|
| `cockpit-hitl.mp4` | Below the HITL screenshot in the Differentiation section | ~30 seconds, 16:9, silent. Capture: agent output enters the Analyst tab, banker clicks Approve / Edit / Reject, the correction flows into a MEMORY file. Single cut, no narration. |

Format: MP4 (H.264 or H.265), 1280x720 or 1920x1080, under 6MB. The video auto-plays muted and loops — design the cut so the loop seam is invisible (end frame ≈ start frame).

The component uses `IntersectionObserver` and only plays when scrolled into view.
